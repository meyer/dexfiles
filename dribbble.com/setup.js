'use strict';

/*
window.dex.config = {
  loggedIn: function() {
    return document.body.classList.contains('logged-in');
  },

  profileURL: function() {
    return window.$('#t-profile>a').attr('href');
  },

  shots: function() {
    let i, len, shot, shotsByID;
    shotsByID = {};
    if (typeof window.SHOTS !== 'undefined') {
      for (i = 0, len = window.SHOTS.length; i < len; i++) {
        shot = window.SHOTS[i];
        shotsByID[shot.id] = shot;
      }
    } else {
      shotsByID = false;
    }
    return shotsByID;
  },
};
*/
